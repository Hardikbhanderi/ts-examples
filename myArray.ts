const superHeros: string[] = [];
const superPower: Array<number> = [];

type User = {
    name: string;
    isActive: boolean;
}

const allUsers : User[] = []
const allTwoUsers : Array<User> = [];

const multiArray: number[][] = [
    [111,333,222],
    // "1"  not allowed
]
 

allUsers.push({name: "hardik", isActive: true})

superHeros.push("antman");
superPower.push(10)
 